{% extends 'index.twig' %}
{% block container %}

    <div class="panel">
        <div class="panel-heading">
            <h3 class="panel-title">     <a href="/Judges/demandes">Judges</a>>>all</h3>
        </div>
        <div class="panel-body">
            <table id="demo-dt-basic" class="table table-striped table-bordered">
                <thead>
                <tr>
                    <th>Type de candidature</th>
                    <th>Nom du projet</th>
                    <th class="min-tablet">Lead</th>
                    <th>soumis le </th>
                    <th>Avis chargé</th>
                    <th class="min-desktop">Action</th>

                </tr>
                </thead>
                <tbody>
                {% for p in Sessions.Project %}
                    <tr>
                        <td>{{ p.createdBy.TypeLabel.type}} </td>
                        <td>{{ p.Name }}</td>
                        <td>{{ p.members[0].LastName}}</td>
                        <td>{{ p.ceatedBy.TypeLabel.SoumissionDate|date }}</td>
                        <td>{{ p.createdBy.review.type}}</td>
                        <td>    <form  >
                                <a href="/Judges/search/{{ d.createdBy.id }}">Juger</a>
                            </form>

                        </td>
                    </tr>
                {% endfor %}


                </tbody>
            </table>



            <a href="/judges/addJudge">Add Judge</a>
            </form>
        </div>
    </div>
{% endblock %}